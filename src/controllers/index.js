const userController = require('./user');
const blogController = require('./blog');
const uploadController = require('./upload');
const paginationController = require('./pagination');

module.exports = {
    userController,
    blogController,
    uploadController,
    paginationController
}